# Optional. Production is the default; use this for staging or local development.
EXPO_PUBLIC_SYNC_API_URL=https://api.sustainabilitywise.com.au

# One-off migration builds only. Normal release builds must leave the enable
# flag false/unset so the public Expo bundle contains no registration secret.
# EXPO_PUBLIC_ENABLE_LEGACY_BOOTSTRAP=false
# EXPO_PUBLIC_REGISTRATION_SECRET=
